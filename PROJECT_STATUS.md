# Repository Status

This initial repository snapshot is a documentation/media deployment of the autonomous rover project.

The original ROS 2 and Arduino source files are intentionally not fabricated or recreated from screenshots. They can be copied into `ros2/` and `arduino/` from the original development workspace when available.
